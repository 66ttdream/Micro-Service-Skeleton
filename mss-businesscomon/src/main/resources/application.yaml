spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/platform?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: platform
    password: 102466
    hikari:
      maximum: 20
      minimum-idle: 5
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update
      #dialect: org.hibernate.dialect.MySQL5InnoDBDialect
